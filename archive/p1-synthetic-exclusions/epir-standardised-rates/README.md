# EpiR Handbook: Directly Standardised Rates

This collection ports the directly standardised point-estimate workflow from the
EpiR Handbook with its exact source CSV files. See `SOURCE_MAP.md` for the bounded
scope and `ATTRIBUTION.md` for the noncommercial ShareAlike terms.

Run the notebook from this directory:

```powershell
bl notebook run lesson.bln
```

Run the golden-value check from `tests`:

```powershell
bl run validate.bl
```
