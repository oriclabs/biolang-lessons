# Attribution

This lesson adapts the Figure 7.2 workflow from Chapter 7 of *Data Integration,
Manipulation and Visualization of Phylogenetic Trees* by Guangchuang Yu. The
online book is licensed under
[CC BY-NC-SA 4.0](https://creativecommons.org/licenses/by-nc-sa/4.0/), and the
adapted instructional material remains under that licence.

The exact `tree_nwk`, `df_info`, `df_alleles`, and `df_bar_data` objects come
from the official `TDbook` companion package at commit
`60b2de7f8df45b440c93cc17513e868ee98c1496`. That package declares the
[Artistic License 2.0](https://opensource.org/license/artistic-2-0). The four
`.rda` files are byte-for-byte repository copies. The browser-readable CSVs are
mechanical exports of those objects, and `snp_data.csv` is a mechanical
execution of the published Figure 7.2 transformation.

The original `tree.nwk`, `info.csv`, `alleles.csv`, and `bar.csv` are also kept
byte for byte from the book repository at commit
`1af8f25551105279a2b8b5b1a449eb002936ce9f`. The bare-CR line endings and
irregular source fields in two of those files are preserved; the lesson reads
the official package-object exports instead of silently rewriting them.

`figure-7.2.png` is the exact published PNG. No synthetic input or independently
rendered substitute is included. Explanatory text and executable checks were
adapted for BioLang by OriClabs on 2026-09-03. This is not an official YuLab
publication or endorsement.

